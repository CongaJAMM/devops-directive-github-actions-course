# System Setup (2.2)


Forked and cloned the repo

    git clone --recurse-submodules https://github.com/CongaJAMM/devops-directive-github-actions-course.git


## Probably leads to issues:

Created conda env w/ requirements.yaml

## Continued

Downloaded DevBox Using `curl -fsSL https://get.jetify.com/devbox | bash`

When prompted selected `Y`

Verified installation with `devbox version`

Navigated back to cloned repo `cd /mnt/d/devops-directive-github-actions-course`

Executed shell `devbox shell`

Made sure that only (devbox) is prepended to terminal: `(devbox) congajamm@CONNB441:/mnt/d/devops-directive-github-actions-course$ `

Installed the YAML and GitHub Actions VS Code Extensions

