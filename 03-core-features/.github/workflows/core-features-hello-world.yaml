# https://courses.devopsdirective.com/github-actions-beginner-to-pro/lessons/03-core-features/01-workflow-anatomy

# All workflows in \.github\workflows

name: Hello World

on:
  workflow_dispatch:

jobs:
  say-hello-inline-bash:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Hello from an inline bash script in a GitHub Action Workflow!"